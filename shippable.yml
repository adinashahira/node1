language: node_js

node_js:
  - 0.12

build:
  ci:
    - npm install ejs
    - npm install
    - npm list ejs
    - npm test
    - echo "Ci succeeded"
  cache: true
  cache_dir_list:
    - $SHIPPABLE_BUILD_DIR/node_modules